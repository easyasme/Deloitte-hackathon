---
phase: "05-2021-prediction-error-analysis"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "05-01"
files_modified:
  - "Task2_Data/task2_step5_metrics.csv"
  - "Task2_Data/task2_step5_error_analysis.csv"
autonomous: true
requirements:
  - "PRED-02"
  - "PRED-03"
  - "DOC-02"
must_haves:
  truths:
    - "2021 predictions evaluated against actual holdout values"
    - "Top 10-20 highest-error ZIPs identified and validated against fire risk"
    - "Error distribution percentiles computed (50th, 75th, 90th, 95th)"
  artifacts:
    - path: "Task2_Data/task2_step5_metrics.csv"
      provides: "RMSE, MAE, MAPE for ensemble and individual models"
      rows: "3+ (Ensemble, PanelFE, LightGBM)"
    - path: "Task2_Data/task2_step5_error_analysis.csv"
      provides: "Top-error ZIPs with fire_risk_score correlation"
      rows: "20+ (top error ZIPs)"
---

<objective>
Evaluate 2021 predictions against holdout actuals, compute error metrics, identify high-error ZIPs, and validate against fire risk distribution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@Task2_Data/task2_step5_predictions.csv
@Task2_Data/task2_step3_baseline_models.py (evaluate_model pattern)
@.planning/phases/05-2021-prediction-error-analysis/05-CONTEXT.md (D-08, D-09, D-10)

## Key Decisions from 05-CONTEXT.md

D-08: Top-N error analysis: identify top 10-20 highest-error ZIP codes by absolute error
D-09: Validate high-error ZIPs against fire risk distribution — do high-error ZIPs correlate with high fire risk?
D-10: Error outputs: both tabular high-error list AND statistical distribution percentiles (50th, 75th, 90th, 95th for RMSE and MAE)
D-11: Compare ensemble predictions vs 2021 actuals AND vs each individual model (PanelFE, LightGBM)
D-14: Primary output: predictions CSV with ZIP, Category, predicted_premium, actual_premium, error, abs_error, fire_risk_score

## Error Analysis Pattern

From task2_step3_baseline_models.py:
```python
def evaluate_regression(model_name, y_true, y_pred):
    rmse = np.sqrt(mean_squared_error(y_true, y_pred))
    mae = mean_absolute_error(y_true, y_pred)
    non_zero_mask = y_true > 0
    mape = np.mean(np.abs((y_true.values[non_zero_mask] - y_pred[non_zero_mask]) / y_true.values[non_zero_mask])) * 100
    return {'model': model_name, 'rmse': rmse, 'mae': mae, 'mape': mape}
```

## Output Structure

Metrics CSV (task2_step5_metrics.csv):
- Columns: model, rmse, mae, mape
- Rows: Ensemble, PanelFE, LightGBM (and optionally LR, RF from Phase 3)

Error Analysis CSV (task2_step5_error_analysis.csv):
- Columns: ZIP, Category, predicted_premium, actual_premium, error, abs_error, fire_risk_score
- Sorted by abs_error descending
- Top 20 rows included
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute error metrics and identify high-error ZIPs</name>
  <files>
    - Task2_Data/task2_step5_error_analysis.py
    - Task2_Data/task2_step5_metrics.csv
    - Task2_Data/task2_step5_error_analysis.csv
  </files>
  <read_first>
    - Task2_Data/task2_step5_predictions.csv
    - Task2_Data/task2_step3_baseline_models.py (evaluate_model pattern)
  </read_first>
  <action>
    Create Task2_Data/task2_step5_error_analysis.py that:

    ## STEP 1: Load predictions
    INPUT = "Task2_Data/task2_step5_predictions.csv"
    df = pd.read_csv(INPUT)
    print(f"Loaded {len(df)} predictions")

    ## STEP 2: Compute error metrics per model (D-11, D-14)
    For each model column (ensemble_pred, panel_fe_pred, lgb_pred):
    - error = predicted - actual
    - abs_error = |error|
    - Compute RMSE = sqrt(mean(error^2))
    - Compute MAE = mean(abs_error)
    - Compute MAPE on non-zero actuals only: mean(|actual - pred| / |actual|) * 100
    - Add actual_premium column to predictions CSV if not present

    ## STEP 3: Save metrics CSV
    METRICS_OUT = "Task2_Data/task2_step5_metrics.csv"
    Columns: model, rmse, mae, mape
    Models: Ensemble, PanelFE, LightGBM

    ## STEP 4: Top-20 highest-error ZIPs (D-08)
    Sort predictions by abs_error descending
    top_errors = df.nlargest(20, 'abs_error')[['ZIP', 'Category', 'predicted_premium', 'actual_premium', 'error', 'abs_error', 'fire_risk_score']]
    
    ## STEP 5: Validate against fire risk distribution (D-09)
    For top 20 high-error ZIPs:
    - Compare their fire_risk_score distribution to overall fire_risk_score distribution
    - Compute: mean fire_risk_score of top 20 vs overall mean
    - Compute: percentile rank of high-error ZIPs in fire_risk_score distribution
    - Answer: Do high-error ZIPs correlate with high fire risk?

    ## STEP 6: Error distribution percentiles (D-10)
    Compute percentile values (50th, 75th, 90th, 95th) for:
    - abs_error (using all rows)
    - Report as: {'percentile': 50, 'abs_error': value} format

    ## STEP 7: Save error analysis CSV
    ERROR_OUT = "Task2_Data/task2_step5_error_analysis.csv"
    - top 20 errors with all columns
    - Append fire_risk_percentile column showing where each ZIP ranks in fire risk distribution

    ## STEP 8: Print summary
    Print metrics table with all models
    Print top 10 high-error ZIPs
    Print fire risk correlation finding

    Print "--- Metrics saved to", METRICS_OUT
    Print "--- Error analysis saved to", ERROR_OUT
  </action>
  <verify>
    <automated>python Task2_Data/task2_step5_error_analysis.py 2>&1</automated>
  </verify>
  <done>
    task2_step5_metrics.csv exists with RMSE, MAE, MAPE for Ensemble, PanelFE, LightGBM
    task2_step5_error_analysis.csv exists with top-20 error ZIPs
    Fire risk correlation finding printed (do high-error ZIPs correlate with high fire risk?)
  </done>
</task>

</tasks>

<verification>
1. Run: python Task2_Data/task2_step5_error_analysis.py
2. Verify task2_step5_metrics.csv has 3+ rows (Ensemble, PanelFE, LightGBM)
3. Verify RMSE values are in reasonable range (similar magnitude to Phase 4 validation RMSE ~494k)
4. Verify task2_step5_error_analysis.csv has top 20 errors sorted by abs_error descending
5. Check fire_risk_percentile column is populated
6. Confirm printed finding about correlation between high-error ZIPs and fire risk
</verification>

<success_criteria>
- task2_step5_metrics.csv reports RMSE, MAE, MAPE for all three models
- task2_step5_error_analysis.csv contains top-20 highest-error ZIPs with fire_risk_score
- Error distribution percentiles (50th, 75th, 90th, 95th) computed and reported
- Fire risk correlation with high-error ZIPs analyzed and printed
- Metrics comparable to Phase 4 validation metrics (RMSE ~494k expected for ensemble)
</success_criteria>

<output>
After completion, create `.planning/phases/05-2021-prediction-error-analysis/05-02-SUMMARY.md`
</output>
